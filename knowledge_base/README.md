# 课程知识库

首版以 `circuit_theory/` 为唯一主知识库；`analog_electronics/` 和 `digital_electronics/` 仅展示未来扩展格式。

上传到星辰 Agent 时建议按课程分别建库，文件名和一级标题保持一致。不要上传 `archive_legacy/`，避免旧架构文档和重复课程材料干扰检索。

知识条目应包含：主题、适用条件、核心关系、常见错误和核验方式。正式扩充前需由课程教师审核。
