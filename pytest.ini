[pytest]
testpaths = apps/api/tests
pythonpath = . apps/api
asyncio_mode = auto
addopts = --cov=app --cov-report=term-missing
markers =
    integration: requires downloaded local models or persistent services
    slow: resource-intensive model inference
