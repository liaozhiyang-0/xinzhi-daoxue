# 93 布线与标签报告

日期：2026-08-26

- 网络线采用水平/垂直折线路由；连续线段不会出现斜线。
- 多端口网络生成显式 junction，网络 ID 通过 `data-wire-net` / `data-junction-net` 保留。
- 器件名、值、网络名和注释属于不同标签类型，未知值显示 `?` 并给出告警。
- 组件和端口通过 `data-component-id` / `data-port` 映射回 IR，便于浏览器审计与后续交互。
