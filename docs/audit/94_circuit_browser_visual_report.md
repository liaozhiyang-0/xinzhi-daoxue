# 94 浏览器电路图报告

日期：2026-08-26

legacy workspace 已提供独立“电路图产物”卡片、拓扑状态、告警列表和缩放工具栏（缩小、适应宽度、放大）。SVG 经浏览器端安全清理后再插入 DOM；`script`、`foreignObject`、事件属性和 `javascript:` 链接会被移除。

当前浏览器回归已确认工作区、任务卡片和缩放控件存在，文本/图片没有可信 `CircuitIR` 时卡片保持“未生成”而不画猜测线路。实际 SVG 卡片展示依赖任务携带可信 IR；不能把自由文本回归误报成渲染器失败。电路卡片样式使用设计 token，支持 light/dark 根主题。
