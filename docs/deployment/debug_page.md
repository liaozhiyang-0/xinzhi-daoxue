# 本地调试页面

访问 `http://localhost:8000/debug` 可使用原生 HTML/CSS/JavaScript 调试会话、附件、非阻塞任务、SSE、取消、重试和产物查询。

这是本地开发调试工具，不是正式学生端。页面不会保存 API Key，也不会直接调用讯飞星辰。

页面和所有 Mock 结果会持续显示：

> 当前为本地 Mock 演示结果，不是讯飞星辰真实输出。

当前附件只在本地 MinIO 或回退目录中保存并关联任务，不上传讯飞星辰。
