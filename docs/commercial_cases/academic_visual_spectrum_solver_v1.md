# academic_visual_spectrum_solver_v1

该场景面向带频谱图的傅里叶变换与正弦调制问题。真实执行必须先使用题图提取频率轴、支撑区间、中心、峰值和搬移位置，再进入频域推导；题图字段不完整时进入人工复核，不得由模型补猜频谱。

验收重点：

- 原图附件和哈希可追踪；
- 视觉验收通过后才能生成解析式；
- `Y(jω)` 的频移、系数正负、非零频带与峰值位置可复核；
- 公式渲染或视觉字段不确定时不可自动发布。
