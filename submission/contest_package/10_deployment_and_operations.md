# 10 部署、权限与运维说明（草案）

## 当前可复核边界

- 本地开发入口：`scripts/dev.ps1`、`scripts/dev.sh`；配置通过环境变量和 `.env` 注入。
- Docker 编排文件为根目录 `docker-compose.yml`，可先用 `docker compose config --quiet` 做静态配置检查。本轮未启动 Docker 容器，因此不记录容器健康、吞吐或部署成功结论。
- 认证开启时，业务身份由会话账号覆盖请求中的 `user_id`；教师/管理员可访问教师工作台、资料复核、评测和反馈统计，学生只能提交和查看自己的学习任务。
- 教师资料支持发布、拒绝和审计状态；任务反馈统计不返回用户 ID。
- 课程复制/新增遵循 `docs/course_pack_design.md` 与 `docs/implementation/skill_registry.md`：新增课程声明 CoursePack、技能和知识目录，不复制冻结求解图，也不把新课程路由到 `SOLVER_CT_V1`。

## 数据与删除边界

- 真实凭据、原始星辰 YAML、学生隐私和未经脱敏的试用记录不得进入提交包。
- 记忆、上传文件和课程资料使用现有删除/撤回接口与发布门禁；提交前仍需由负责人补充学校数据保留期限、导出格式和授权记录。
- 任何真实 Provider 调用都必须有单独授权、完整配置和可追溯运行记录；本提交包中的 Mock/离线结果不能替代云端或用户结果。

## 待补充门禁

1. 运行一次授权环境中的 `docker compose config --quiet`，保存配置检查记录；容器启动结果另行记录。
2. 补充学校级数据删除、导出、脱敏和版权/授权审批单。
3. 补充教师、学生、管理员三类账号的脱敏验收记录。
4. 负责人完成三个演示案例后，再补充案例输入、预期输出和三分钟讲解脚本。
