# 02 伦理与安全边界

- 不提交真实 API Key、Secret、Flow ID、学生隐私或未脱敏数据。
- 未经授权不执行真实 Provider 调用；真实云端结果必须有单独授权和运行记录。
- Mock、离线评测和本地确定性结果必须显式标记，不能表述为真实用户效果。
- 教学建议保留教师复核边界，不自动生成正式成绩或竞赛结论。
- 教师上传资料只在获得授权后进入课程资产；发布、撤回和索引状态必须可审计。
- `SOLVER_CT v1.0`/`SOLVER_CT_V1` 只作为退役历史记录引用，不在本材料包中恢复或调用。
