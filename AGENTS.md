# Repository Engineering Rules

1. 变更前检查结构、配置、数据流、依赖和入口，不重写无关部分。
2. 使用小而清晰的模块、类型注解和可扩展配置，避免硬编码与重复逻辑。
3. 路径、密钥、连接串和运行参数使用环境变量或命令行参数。
4. 实验与测试必须给出可复现命令、输入、输出、日志位置和关键指标。
5. 修改后运行相应测试和静态检查；未执行的验证必须明确说明。
6. 报告变更文件、原因、运行方式、验证结果、风险和下一步。
7. 不虚构代码运行、测试、指标、日志或外部 API 字段。
8. 新功能应允许后续添加课程、Agent、Provider、模型、存储和任务后端。
9. `SOLVER_CT v1.0` 是冻结基线，不直接修改。
10. 当前工作流尚未发布外部 API，本阶段不得实现真实星辰网络调用。
11. 原始星辰 YAML 只放在 `.local_inputs/`，不得进入公共仓库。
12. Mock 结果必须显式标记，不得描述为真实工作流结果。
13. 不在 `archive_legacy/` 中开发新功能，不大规模移动课程资料。
14. 数据库变化必须增加增量 migration，不修改已提交 migration。
15. 事件协议变化必须增加 SSE 顺序与重连测试。
16. 任务创建必须保持非阻塞，路由不得直接执行 Provider。
17. Windows PowerShell 是主要本地环境，同时保持 Linux/macOS 脚本可用。
18. Docker 未执行或不可用时必须明确报告。
19. 每次提交前运行 Ruff、Mypy、Pytest、配置和敏感文件检查。
20. 不自动合并 PR、不 force push、不提交学生隐私或真实密钥。
21. 不虚构准确率、性能或星辰字段；真实 API 接入等待后续独立阶段。
